import { StyleSheet, Text, View } from "react-native";
import React from "react";
import NavStack from "./stack/NavStack";

const App = () => {
  return (
    <View>
      <NavStack />
      {/* <loginscreen>
        <form>
          <input />
          <input />
          <button />
        </form>
      </loginscreen>
<stack>
if login ==> then successscreen
      if not ===> then loginscreen

</stack> */}
    </View>
  );
};

export default App;

const styles = StyleSheet.create({});
